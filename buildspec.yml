version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9

  build:
    commands:
      - echo "Build started"
      - echo "Listing files"
      - ls -la

artifacts:
  files:
    - '**/*'
